== README

This is a basic blogger app, generated with rails g scaffold.
I then implemented Devise with Facebook OAuth on top of that. Afterwards, I deployed to AWS Elastic Beanstalk.

=NEXT STEPS
-Implement a logout button
-Add CSS
-Add HTTPS & re-deploy to Elastic Beanstalk.
